import styled from 'styled-components';

export const StyledLogo = styled.div`
    width: 50px;
    max-width: 100%;
    height: auto;
    border-radius: 50px;
    overflow: hidden;
    z-index: var(--stack-level-1);

    img {
        width: 100%;
    }
`;
